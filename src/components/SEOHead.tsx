type SEOHeadProps = {
  title: string;
  description: string;
  canonical?: string;
  robots?: string;
};

export function SEOHead({ title, description, canonical, robots }: SEOHeadProps) {
  return null;
}
